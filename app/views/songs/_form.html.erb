
<div class-"field">
  <label for "sc-search"> search soundCloud </label>
  <input id="sc-search" type="text"/>
  <button id="sc-search-bttn"> search </button>
</div>

<!--   <div class="row"> -->
    <div id="sc-results"> <!-- class="small-2 column"> -->
    </div>
<!--   </div> -->


<%= form_for(@song) do |f| %>
  <% if @song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@song.errors.count, "error") %> prohibited this song from being saved:</h2>

      <ul>
      <% @song.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :playlist_id %>
    <%= f.collection_select(:playlist_id, Playlist.all, :id, :title, prompt: true) %>
  </div>

   <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="field hidden">
    <%= f.label :artist %>
    <%= f.text_field :artist %>
  </div>
  <div class="field hidden">
    <%= f.label :album %>
    <%= f.text_field :album %>
  </div>
  <div class="field hidden">
    <%= f.label :sc_id %>
    <%= f.text_field :sc_id %>
  </div>
  <div class="field hidden">
    <%= f.label :sc_permalink %>
    <%= f.text_field :sc_permalink %>
  </div>
  <div class="field hidden">
    <%= f.label :sc_permalink_url %>
    <%= f.text_field :sc_permalink_url %>
  </div>
  <div class="field hidden">
    <%= f.label :sc_uri %>
    <%= f.text_field :sc_uri %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>